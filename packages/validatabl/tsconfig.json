{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "composite": true,
    "rootDir": "src",
    "outDir": "dist",

    // 👇 override these for this Node library
    "module": "NodeNext",
    "moduleResolution": "NodeNext"
    // (you could also use "node" here; "NodeNext" is fine with ESNext modules)
  },
  "include": ["src"],
  "references": [
    { "path": "../validatabl-core" }
  ]
}
